<div class="container">
  <h2><%=category %> coupons</h2>
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Store</th>
        <th>Description</th>
        <th>Category</th>
        <th>Price</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      <% coupons.forEach(function(coupon){ %>
      <tr>
        <td><%= coupon.get("storeName") %></td>
        <td><%= coupon.get("couponDescription") %></td>
        <td><%= coupon.get("category") %></td>
        <td><%= coupon.get("price") %></td>
        <td><a href="/coupons/coupon?id=<%=coupon.id%>" class="btn btn-primary" role="button">Learn More</a></td>
      </tr>
      <% });%>
    </tbody>
  </table>
</div>
